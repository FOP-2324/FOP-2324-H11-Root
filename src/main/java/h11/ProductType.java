package h11;

import org.tudalgo.algoutils.student.annotation.DoNotTouch;

// Enum with two values? Either add another one or replace by a boolean.
@DoNotTouch
public enum ProductType {
    Hardware,
    Software
}
